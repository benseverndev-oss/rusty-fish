[package]
name = "engine-bench"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
engine-core = { path = "../engine-core" }
engine-search = { path = "../engine-search" }
